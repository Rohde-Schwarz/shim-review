diff -Nabur a/shim-16.1/include/verify.h b/shim-16.1/include/verify.h
--- a/shim-16.1/include/verify.h	2025-07-30 09:54:28.609297919 +0200
+++ b/shim-16.1/include/verify.h	2025-08-01 08:50:01.845033304 +0200
@@ -28,14 +28,14 @@
  * with a trusted key.
  */
 EFI_STATUS
-shim_verify(void *buffer, UINT32 size);
+__attribute__((ms_abi)) shim_verify(void *buffer, UINT32 size);
 
 EFI_STATUS
-shim_hash(char *data, int datasize, PE_COFF_LOADER_IMAGE_CONTEXT *context,
+__attribute__((ms_abi)) shim_hash(char *data, int datasize, PE_COFF_LOADER_IMAGE_CONTEXT *context,
           UINT8 *sha256hash, UINT8 *sha1hash);
 
 EFI_STATUS
-shim_read_header(void *data, unsigned int datasize,
+__attribute__((ms_abi)) shim_read_header(void *data, unsigned int datasize,
                  PE_COFF_LOADER_IMAGE_CONTEXT *context);
 
 // vim:fenc=utf-8:tw=75:noet
diff -Nabur a/shim-16.1/shim.h b/shim-16.1/shim.h
--- a/shim-16.1/shim.h	2025-07-30 09:54:28.613297919 +0200
+++ b/shim-16.1/shim.h	2025-08-01 08:49:37.180031982 +0200
@@ -236,9 +236,9 @@
 	);
 
 typedef struct _SHIM_LOCK {
-	EFI_SHIM_LOCK_VERIFY Verify;
-	EFI_SHIM_LOCK_HASH Hash;
-	EFI_SHIM_LOCK_CONTEXT Context;
+	__attribute__((ms_abi)) EFI_SHIM_LOCK_VERIFY Verify;
+	__attribute__((ms_abi)) EFI_SHIM_LOCK_HASH Hash;
+	__attribute__((ms_abi)) EFI_SHIM_LOCK_CONTEXT Context;
 } SHIM_LOCK;
 
 extern EFI_STATUS shim_init(void);
diff -Nabur a/shim-16.1/verify.c b/shim-16.1/verify.c
--- a/shim-16.1/verify.c	2025-07-30 09:54:28.631297920 +0200
+++ b/shim-16.1/verify.c	2025-08-01 08:49:51.272032737 +0200
@@ -753,7 +753,7 @@
  * buffer is signed with a trusted key.
  */
 EFI_STATUS
-shim_verify(void *buffer, UINT32 size)
+__attribute__((ms_abi)) shim_verify(void *buffer, UINT32 size)
 {
 	EFI_STATUS efi_status = EFI_SUCCESS;
 	PE_COFF_LOADER_IMAGE_CONTEXT context;
@@ -799,7 +799,7 @@
 }
 
 EFI_STATUS
-shim_hash(char *data, int datasize, PE_COFF_LOADER_IMAGE_CONTEXT *context,
+__attribute__((ms_abi)) shim_hash(char *data, int datasize, PE_COFF_LOADER_IMAGE_CONTEXT *context,
           UINT8 *sha256hash, UINT8 *sha1hash)
 {
 	EFI_STATUS efi_status;
@@ -816,7 +816,7 @@
 }
 
 EFI_STATUS
-shim_read_header(void *data, unsigned int datasize,
+__attribute__((ms_abi)) shim_read_header(void *data, unsigned int datasize,
                  PE_COFF_LOADER_IMAGE_CONTEXT *context)
 {
 	EFI_STATUS efi_status;
